(ns aoc2019.day-6
  (:gen-class)
  (:require [clojure.string :as str]))

(defn part-1 [orbits] nil)

(defn part-2 [orbits] nil)

(defn parse [string] nil)

(defn get-input [] nil)

(defn -main [& args]
  (let [input (get-input)]
    (println (part-1 input))
    (println (part-2 input))))
